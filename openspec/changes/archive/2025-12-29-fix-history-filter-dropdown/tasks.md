# 任务列表

## 1. 移除硬编码的筛选选项
- [x] 在 `tools.html` 中，移除 `history-filter` 下拉框中硬编码的 `<option>` 元素
- [x] 保留"全部工具"选项（value=""）作为第一个选项

## 2. 实现动态生成筛选选项
- [x] 在 `tools.js` 中添加函数 `renderHistoryFilter()`，根据 `TOOLS` 数组生成筛选选项
- [x] 函数应该遍历 `TOOLS` 数组，为每个工具创建一个 `<option>` 元素
- [x] 每个选项的 `value` 设置为工具的 `id`，显示文本设置为工具的 `name`

## 3. 初始化筛选下拉框
- [x] 在 `DOMContentLoaded` 事件中调用 `renderHistoryFilter()` 函数
- [x] 确保在绑定筛选事件监听器之前生成选项

## 4. 测试和验证
- [x] 验证所有工具都出现在筛选下拉框中
- [x] 验证筛选功能能够正确过滤历史记录
- [x] 验证"全部工具"选项能够显示所有历史记录
- [x] 验证每个工具选项能够正确筛选对应类型的历史记录

